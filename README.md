# brro-fx
Brro FX is a lightweight WordPress plugin that adds class-based smooth scroll and scroll/entrance motion effects using vanilla JavaScript, Intersection Observer, and CSS custom properties (no jQuery required).
